# Components 로드맵

우선순위: `P0` 결함·안전성 → `P1` 핵심 구조 → `P2` API·성능 → `P3` 장기 확장

## 작업 순서

1. **P0-01 — 입력 이벤트 인덱스 처리 검증**
   - 첫 번째 이벤트를 찾았을 때도 중복 생성되지 않도록 검색 조건과 테스트를 확인합니다.
2. **P1-01 — Input System 기반 입력 구조로 전환**
   - `InputActionReference`와 콜백 기반 구성으로 대체 가능한 범위를 확인합니다.
   - 기존 컴포넌트가 제공하는 추가 가치가 없으면 축소하거나 제거합니다.
3. **P1-02 — SerializeReference 의존성 정리**
   - 선택적 define에 따른 API 누락과 설치 경험을 문서화하고 대체 Inspector를 검토합니다.
4. **P2-01 — Movement 컴포넌트 계약 통합**
   - Transform·Vector 이동 구현의 중복, 시간 기준, 취소 및 완료 이벤트를 정리합니다.
5. **P3-01 — MonoBehaviour 기능의 조립형 구성**
   - 재사용 가능한 동작을 순수 전략 객체와 얇은 컴포넌트로 분리할지 검토합니다.
